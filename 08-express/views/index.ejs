<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('./includes/head.ejs', {title: '홈'}) %>
  </head>
  <body>
    <%- include('./includes/header.ejs') %>
    <h1>Hello! Express!!</h1>
    <p>하이~~~~~~~ <%= userId%>님 환영합니다!</p>
    <p>당신의 패스워드는 <%= userPw%>맞나요?</p>
    <% for (let i=0; i<5; i++) { %>
    <div><%= i%></div>
    <% } %>
    <!-- ejs 내부에서 변수를 선언해서 사용하면 -->
    <!-- 프론트 쪽 변수 선언이 먹어서 백쪽 변수를 사용하지 않게 된다! -->
    <!-- 같은 변수 이름으로 설정해서 나는 오류가 아님!  -->
    <% for (let btn of btns) { %>
    <button type="button" class="btn btn-primary"><%= btn %></button>
    <% } %>
    <button type="button" class="btn">
      <a href="/login">로그인</a>
    </button>
    <button type="button" class="btn"><a href="/register">회원가입</a></button>

    <br />
    <span><%= me.name%></span>
    <br />
    <span><%= me.msg%></span>

    <br />
    <% if (!isLogin) { %>
    <h4>아직 로그인을 하지 않으셨군요!</h4>
    <% } else { %>
    <h4>로그인을 하셨군요!</h4>
    <% } %>
  </body>
</html>
