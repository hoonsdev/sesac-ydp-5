import React from 'react';

export default function SearchUser() {
  return (
    <div>
      <select>
        <option value="">작성자</option>
      </select>
      <input type="" />
      <button>검색</button>
    </div>
  );
}
